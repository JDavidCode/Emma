[
    {
        "name": "server_shutdown",
        "description": "stop the app (go sleep, rest)",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    },
    {
        "name": "start_thread",
        "description": "Start a new thread or process",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The name of the thread or process"
                }
            },
            "required": ["name"]
        }
    },
    {
        "name": "stop_thread",
        "description": "Stop a running thread or process",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The name of the thread or process"
                }
            },
            "required": ["name"]
        }
    },
    {
        "name": "restart_thread",
        "description": "Restart a specific thread or process",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The name of the thread or process"
                }
            },
            "required": ["name"]
        }
    },
    {
        "name": "module_reloader",
        "description": "Reload or refresh specific modules or libraries",
        "parameters": {
            "type": "object",
            "properties": {
                "module_name": {
                    "type": "string",
                    "description": "The name of the module or library to reload"
                }
            },
            "required": ["module_name"]
        }
    },
    {
        "name": "open_app",
        "description": "Open a specific application",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The name of the application to open"
                }
            },
            "required": ["name"]
        }
    },
    {
        "name": "volume_management",
        "description": "Adjust the volume",
        "parameters": {
            "type": "object",
            "properties": {
                "action": {
                    "type": "string",
                    "enum": ["up", "down"],
                    "description": "The volume action to perform (up or down)"
                }
            },
            "required": ["action"]
        }
    },
    {
        "name": "youtube_player",
        "description": "Search for a video on YouTube",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "The search query for the YouTube video e.g. music, a name, a phrase"
                }
            },
            "required": ["query"]
        }
    },
    {
        "name": "google_search",
        "description": "Perform a search on Google",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "The search query for Google, e.g. a question, a person name, a phrase"
                }
            },
            "required": ["query"]
        }
    },
    {
        "name": "open_website",
        "description": "Open a specific website",
        "parameters": {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string",
                    "description": "The name of the website to open, e.g. facebook, twitter"
                }
            },
            "required": ["name"]
        }
    },
    {
        "name": "open_local_site",
        "description": "Open a specific local website",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    },
    {
        "name": "weather",
        "description": "Get the weather information",
        "parameters": {
            "type": "object",
            "properties": {
                "args": {
                    "type": "integer",
                    "description": "The number of days to forecast (optional)"
                }
            }
        }
    },
    {
        "name": "date_clock",
        "description": "Get the date and time information",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    },
    {
        "name": "day_parts",
        "description": "Get the parts of the day (morning, afternoon, etc.)",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    }
]
